for i in $(seq -w 13 24);
#for i in 16;
do
  #sbatch isomut2_batch.sh E21000${i} E21000${i}.bam
  sbatch isomut2_batch.sh E21000${i} "E21000${i}.SSAE8.sorted.merged.bam"
  #echo "'E21000${i}.SSAE8.sorted.merged.bam'"
  sleep 0.5
done
